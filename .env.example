# Firebase Configuration for Cloud Storage
# Copy this file to .env and fill in your Firebase credentials
# See CLOUD_STORAGE_SETUP.md for detailed instructions

# Get these values from Firebase Console -> Project Settings -> Your apps -> Web app
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_DATABASE_URL=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Note: If these are not set, the app will fall back to localStorage
# which works great for development but doesn't sync across devices

# Admin Credentials (KEEP PRIVATE - never commit to git!)
# Format: username:password:displayName:role (separated by | for multiple users)
# Example: admin1:password123:Admin One:admin|admin2:pass456:Admin Two:admin
VITE_ADMIN_USERS=
